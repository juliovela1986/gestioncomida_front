name: gestioncomida_front
description: "Organizador de alimentos"
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^8.0.0
  dio: ^5.4.0
  webview_flutter: ^4.0.0
  crypto: ^3.0.3 # Añadido para PKCE
  image_picker: ^1.0.4
  image: ^4.1.3
  intl: ^0.18.0
  file_picker: ^8.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^1.1.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
